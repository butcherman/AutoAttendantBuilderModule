// const dotenvExpand = require('dotenv-expand');
// dotenvExpand(require('dotenv').config({ path: '../../.env'/*, debug: true*/}));

// const mix = require('laravel-mix');
// require('laravel-mix-merge-manifest');

// // mix.setPublicPath('../../public').mergeManifest();

// mix.js(__dirname + '/Resources/assets/js/app.js', 'js/autoattendantbuildermodule.js')
//     .sass( __dirname + '/Resources/assets/sass/app.scss', 'css/autoattendantbuildermodule.css')
//     .setPublicPath('../../public').mergeManifest();

// if (mix.inProduction()) {
//     mix.version();
// }

/**
 * Webpack is not needed here as scripts are compiled from the main webpack file
 */
